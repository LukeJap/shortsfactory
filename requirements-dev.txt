# Development-only dependencies -- not needed to run the app, only to work
# on it (running the test suite, static checks). Install alongside
# requirements.txt: pip install -r requirements.txt -r requirements-dev.txt

pytest==9.1.1
pyflakes==3.4.0
