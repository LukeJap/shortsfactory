"""
One mixin per functional area of ShortsFactoryWindow (playback,
emoji/caption preview, render pipeline, settings, etc.) --
composed together via multiple inheritance in main_window.py. See each
mixin file's own module docstring for what it owns.
"""
