Repair the prior response for the named stage.

Use the validation errors to correct JSON syntax, required fields, value types,
beat references, grounding, or schema shape. Preserve valid content when
possible. The original task and verified source remain authoritative. Do not
invent facts or silently switch to template narration.
